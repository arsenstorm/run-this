{
	"scripts": {
		"build": "cargo build",
		"build:release": "bun lint && bun format && cargo build --release",
		"install": "cargo install --path .",
		"setup": "bun run build:release && bun run install",
		"format": "cargo fmt",
		"test": "cargo test",
		"lint": "cargo clippy --fix --allow-dirty --allow-staged"
	}
}
